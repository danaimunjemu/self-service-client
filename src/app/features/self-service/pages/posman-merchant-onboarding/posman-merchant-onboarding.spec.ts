import { PosmanMerchantOnboarding } from './posman-merchant-onboarding';

describe('PosmanMerchantOnboarding', () => {
  it('should create an instance', () => {
    expect(new PosmanMerchantOnboarding()).toBeTruthy();
  });
});
